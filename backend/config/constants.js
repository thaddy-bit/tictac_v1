/**
 * Centralized Constants for TICTAC Backend
 */

const PLATFORM_CONSTANTS = {
  SEARCH_FEE: 300,
  SESSION_EXPIRY_HOURS: 24,
  MAX_HISTORY_LIMIT: 20,
  POLLING_INTERVAL: 3000,
  MTN_MOMO_CURRENCY: 'XAF', // Central Africa Franc
};

module.exports = PLATFORM_CONSTANTS;
